<?xml version="1.0" encoding="UTF-8"?> 
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd"> 
<mapper namespace="schedule">

	<!-- 리스트 조회 -->
	<select id="selectScheduleList" parameterType="hashmap" resultType="hashmap"> 
			SELECT IDX as idx
				 , title
			 	 , COLOR as color
			 	 , date_format(START_DATE, '%Y-%m-%d-%T') as start_date
				 , date_format(END_DATE, '%Y-%m-%d-%T') AS end_date
				 , date_format(START_DATE, '%Y-%m-%d') AS start
				 , date_format(END_DATE, '%Y-%m-%d') AS end
				 , category
				 , memo
				 , done_flag
			  FROM schedule 
			<where>
				<if test='IDX != "" and IDX != null '>
					AND IDX = #{IDX}  
				</if>	  	  
			</where>
	</select>
	
	<select id="selectScheduleOne" parameterType="hashmap" resultType="hashmap"> 
		<![CDATA[ 
			SELECT title
			 	 , COLOR as color			     
				 , date_format(START_DATE, '%Y-%m-%d') AS start
				 , date_format(END_DATE, '%Y-%m-%d') AS end
			  FROM schedule 
			 WHERE IDX = #{IDX}  
		]]> 
	</select>
	
    <insert id="insertSchedule" parameterType="hashmap">
        <selectKey keyProperty="IDX" resultType="int" order="BEFORE">
        	SELECT IFNULL(MAX(IDX),0)+1 FROM schedule
        </selectKey>
        INSERT INTO schedule
             ( IDX, START_DATE, END_DATE, TITLE, CATEGORY, COLOR, MEMO, DONE_FLAG   
             )
        VALUES
             ( #{IDX}, #{START_DATE}, #{END_DATE}, #{TITLE}, #{CATEGORY}, #{COLOR}, #{MEMO}, 'N'             
             ) 
    </insert>
    
    <update id="updateSchedule" parameterType="hashmap">
        UPDATE schedule
           SET TITLE = #{TITLE}
             , MEMO = #{MEMO}
             , CATEGORY = #{CATEGORY}
             , START_DATE = #{START_DATE}
             , END_DATE = #{END_DATE}
             , COLOR = #{COLOR}
         WHERE IDX = #{IDX}
    </update>    
    
    <delete id="deleteSchedule" parameterType="hashmap">
        DELETE FROM schedule
         WHERE IDX = #{IDX}
    </delete>    
    
<!--     <update id="updateBoard" parameterType="hashmap">
        UPDATE board
           SET TITLE = #{TITLE}
             , CONTENTS = #{CONTENTS}
             , UPDATE_ID = 'testid2'
             , UPDATE_DATETIME = SYSDATE()
         WHERE IDX = #{IDX}
    </update>
    
    <update id="updateBoardHit" parameterType="hashmap">
        UPDATE board
           SET HITCNT = IFNULL(HITCNT,0) + 1
         WHERE IDX = #{IDX}
    </update>    
    
    <delete id="deleteBoard" parameterType="hashmap">
        DELETE FROM board
         WHERE IDX = #{IDX}
    </delete> -->


</mapper>
